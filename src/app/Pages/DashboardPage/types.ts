export interface DefaultLayoutProps {
  children: React.ReactNode;
  allowedRole?: string;
}

export interface pharmacy {
  _id: string;
  name: string;
  logo: string;
  email: string;
  phoneNumber: string;
}
